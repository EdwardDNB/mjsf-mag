# Лабораторна робота №2. Використання Slots та Provide/Inject у Vue 3

## Мета
1. Навчитися передавати дані між компонентами за допомогою **provide/inject**.
2. Ознайомитись із можливостями **slots** у Vue 3.
3. Реалізувати компонент **Tabs**, де дочірні компоненти, що автоматично реєструються через `provide/inject`.


## Засоби та інструменти

- **Vue 3**
- **Tailwind CSS**
-  [Tailwind Tabs components](https://tailwindcss.com/plus/ui-blocks/application-ui/navigation/tabs)


## 📋 Завдання

1. Ознайомся з прикладом реалізації **Stepper** компоненту
2. Ознайомитися з прикладами `Tabs` [компонентів створених з використанням tailwind](https://tailwindcss.com/plus/ui-blocks/application-ui/navigation/tabs) .
3. Створити компонент Tabs за наступними вимогами:
   - Використовує **provide** для передачі:
       - активного `slug`
       - методу `setActiveTab(slug)`
   - Пропси:
       - `modelValue`: активна вкладка (v-model)
       - `variant`: `'underline' | 'pills' | 'boxed'`
   - Відповідає за відображення списку вкладок і логіку перемикання.
4. Створити компонент `Tab.vue`за наступними вимогами:
   - Реєструється автоматично у `Tabs` через **inject**.
   - Має пропси:
       - `title`: назва вкладки
       - `slug`: унікальний ідентифікатор вкладки
   - Відображає контент лише якщо вкладка активна.
5. Протестувати роботу декількох компонентів на одній сторінці
```vue
<!-- TabsDemo.vue -->
<template>
  <Tabs v-model="current" variant="underline">
    <Tab title="Профіль" slug="profile">Вміст профілю...</Tab>
    <Tab title="Налаштування" slug="settings">Форма налаштувань...</Tab>
    <Tab title="Контакти" slug="contacts">Контактна інформація...</Tab>
  </Tabs>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import Tabs from './Tabs.vue'
import Tab from './Tab.vue'
const current = ref('profile')
</script>
```
6. Розмістити проєкт у системі контролю версій та надати посилання на нього у якості звіту з роботи.

## Контрольні питання

- Що таке slot у Vue.js і які вони бувають?
- Як передати змінні у scoped slot?
- Чим provide/inject відрізняється від props/emits?
- Які обмеження має provide/inject?
- Як можна комбінувати slots та provide/inject для створення гнучких компонентів?
- Як реалізувати різні стилі вкладок у Tailwind CSS?