# 📘 Лекція №4. Використання роутів у проєкті Vue Router

## 🎯 Мета заняття

1. Ознайомитись із концепцією маршрутизації у Vue 3.
2. Навчитись створювати навігацію між сторінками без перезавантаження.
3. Зрозуміти основні можливості **Vue Router**:
    - динамічні маршрути;
    - вкладені маршрути;
    - передача параметрів;
    - навігаційна охорона (guards).

---

## 🔍 1. Що таке маршрутизація (Routing)

**Маршрутизація (routing)** — це механізм, який дозволяє керувати тим, який компонент відображається залежно від URL у браузері.

📌 У класичних сайтах при переході між сторінками браузер повністю перезавантажує сторінку.  
📌 У SPA (Single Page Application) з **Vue Router** перехід між сторінками відбувається **без перезавантаження** — Vue просто замінює компонент у межах сторінки.

---

## ⚙️ 2. Встановлення Vue Router

```bash
npm install vue-router
```
Після встановлення — створимо файл router/index.ts (або .js):
```ts
// src/router/index.ts
import { createRouter, createWebHistory } from 'vue-router'
import HomeView from '../views/HomeView.vue'
import AboutView from '../views/AboutView.vue'

const routes = [
  { path: '/', component: HomeView, name: 'home' },
  { path: '/about', component: AboutView, name: 'about' },
]

export const router = createRouter({
  history: createWebHistory(),
  routes,
})
```
Далі потрібно підключити роутер у main.ts:

```ts
// src/main.ts
import { createApp } from 'vue'
import App from './App.vue'
import { router } from './router'

const app = createApp(App)
app.use(router)
app.mount('#app')
```
## 3. Використання маршрутизатора в шаблоні
```vue
<!-- App.vue -->
<template>
  <div>
    <nav class="flex gap-4 p-4 bg-gray-100">
      <RouterLink to="/">Головна</RouterLink>
      <RouterLink to="/about">Про нас</RouterLink>
    </nav>
    <!-- Тут відображатимуться всі сторінки -->
    <RouterView />
  </div>
</template>
```
📌 Компонент <RouterLink> замінює звичайний тег <a>
📌 <RouterView> — це “місце”, де Vue Router рендерить поточний маршрут.

## 🔀 4. Динамічні маршрути
Іноді потрібно, щоб сторінка приймала параметри — наприклад, /user/5.

```ts
const routes = [
  { path: '/user/:id', component: () => import('../views/UserView.vue') },
]
```
Отримати параметр у компоненті можна через useRoute():

```vue
<script setup lang="ts">
import { useRoute } from 'vue-router'
const route = useRoute()
const userId = route.params.id
</script>
```        
## 🧭 5. Навігація з коду
Окрім <RouterLink>, можна переходити між сторінками програмно:

```ts
import { useRouter } from 'vue-router'

const router = useRouter()
router.push('/about')
```        
або з параметрами:
```ts
router.push({ name: 'user', params: { id: 5 } })
```

## 🧱 6. Вкладені маршрути (Nested Routes)

Вкладені маршрути дозволяють створити логічну структуру сторінок.

```ts
const routes = [
  {
    path: '/dashboard',
    component: () => import('../views/DashboardView.vue'),
    children: [
      { path: 'stats', component: () => import('../views/DashboardStats.vue') },
      { path: 'settings', component: () => import('../views/DashboardSettings.vue') },
    ],
  },
]
```    

```vue
<!-- DashboardView.vue -->
<template>
  <div>
    <h1>Панель керування</h1>
    <RouterView /> <!-- Тут буде підмаршрут -->
  </div>
</template>
```
## 🛡️ 7. Навігаційна охорона (Navigation Guards)

Guard дозволяє перевірити, чи можна перейти на певний маршрут.

Локальний guard (у маршруті):
```ts
{
  path: '/profile',
  component: () => import('../views/ProfileView.vue'),
  beforeEnter: (to, from, next) => {
    if (!localStorage.getItem('token')) next('/login')
    else next()
  }
}
```
Глобальний guard:

```ts
router.beforeEach((to, from, next) => {
  console.log('Переходимо на:', to.path)
  next()
})
```
## 🧠 8. Робота з Layouts
Layout — це спільна оболонка для групи сторінок (наприклад, з меню та хедером).

```vue
<!-- DefaultLayout.vue -->
<template>
  <header>Моє меню</header>
  <main><RouterView /></main>
</template>
```
```ts
const routes = [
  {
    path: '/',
    component: () => import('../layouts/DefaultLayout.vue'),
    children: [
      { path: '', component: () => import('../views/HomeView.vue') },
      { path: 'about', component: () => import('../views/AboutView.vue') },
    ],
  },
]
```    
## 📚 9. Домашнє завдання
Додати Vue Router до вашого проєкту.

Створити три сторінки:

- / — Головна
- /products — Список товарів
- /products/:id — Деталі товару
- Використати <RouterLink> для навігації.
- Реалізувати динамічний маршрут для відображення конкретного продукту.
- (⭐ додатково) Створити layout із хедером і футером.

❓ Контрольні питання
- Що таке Vue Router і для чого він використовується?
- Як оголошуються маршрути у Vue Router?
- Чим відрізняються RouterLink і RouterView?
- Як передати параметри маршруту в компонент?
- Як створити вкладені маршрути?
- Як працюють навігаційні guard-и?
- Як організувати структуру Layouts у додатку?

## 🧩 Приклади для демонстрації на парі
Простий роутер між сторінками Home / About
Сторінка користувача з параметром /user/:id
Вкладені маршрути Dashboard
Навігаційний guard для захисту сторінки профілю

## 🏁 Підсумок
- Vue Router — офіційна бібліотека маршрутизації для Vue 3.
- Дає змогу створювати SPA з декількома сторінками без перезавантаження.
- Підтримує вкладені маршрути, параметри, навігаційні події та охорону доступу.